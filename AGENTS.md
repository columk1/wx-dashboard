# A weather dashboard for local pilots

## Rules
- Use pnpm for package management
- Assume there is always a dev server running at localhost:3000
- Don't delete comments unless they were used temporarily for debugging
